% -------------------------------------------------------------------
% Run this sketch to set the matlab workspace (directory) to scr folder
% so the generated code will be in the right place
open("ControlLoop.slx");
open("StateflowBlock.slx");
